tasks:
  Tests:
    command:
      - python
      - -m
      - robot
      - --report
      - NONE
      - --outputdir
      - ./output
      - --logtitle
      - example Tests log
      - tests/acceptance

artifactsDir:
  output